// Główny plik skryptu dla nakładki StreamToEarn

document.addEventListener("DOMContentLoaded", () => {
    console.log("Nakładka StreamToEarn została pomyślnie załadowana.");

    // Opcjonalnie: Możesz tutaj dodać funkcję pomocniczą, 
    // jeśli potrzebujesz obsługiwać niestandardowe zdarzenia w JavaScript.
});